package edu.channel4.mm.db.android.activity;

import android.app.Activity;
import android.os.Bundle;

public class EditGraphRequestActivity extends Activity {

   // TODO(mlerner): Accept a GraphRequest through the Intent as an Extra
   // Depending on its interfaces, turn on or off different UI elements and
   // hook them up to the relevant methods within the GraphRequest. Provide
   // some sort of "Done" button that does validation -- defaults should be
   // "please select one item" or something similar. Then, create a
   // GraphViewerActivity and put in as an extra the URIparameters. Execute the
   // Intent.

   /** Called when the activity is first created. */
   @Override
   public void onCreate(Bundle savedInstanceState) {
      super.onCreate(savedInstanceState);

      // TODO Auto-generated method stub
   }

}
